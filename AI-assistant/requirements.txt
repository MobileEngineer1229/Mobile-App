# =============================================================================
# Joseon Dynasty artificial intelligence assistant — dependent libraries
# =============================================================================

# -----------------------------------------------------------------------------
# core (Core)
# -----------------------------------------------------------------------------
# RTX 5070 (Blackwell sm_120) When used PyTorchrequires separate installation:
#   pip install torch>=2.7 --index-url https://download.pytorch.org/whl/cu128
# general CUDA GPU:
#   pip install torch>=2.7
torch>=2.7
numpy>=1.26
pyyaml>=6.0

# -----------------------------------------------------------------------------
# ticket analyzer (Tokenizer)
# -----------------------------------------------------------------------------
sentencepiece>=0.2.0

# -----------------------------------------------------------------------------
# learning (Training)
# -----------------------------------------------------------------------------
tensorboard>=2.16
tqdm>=4.66

# -----------------------------------------------------------------------------
# inference / screen (Inference / UI)
# -----------------------------------------------------------------------------
gradio>=6.0

# -----------------------------------------------------------------------------
# Reading multi-format material (Multi-format Data Readers)
# -----------------------------------------------------------------------------

# PDF Extract file text
# use: .pdf file learning material
pdfplumber>=0.11

# Reading Microsoft Word Documents
# use: .docx file learning material
python-docx>=1.1

# image OCR (optical character recognition) — Joseon language + English support
# use: .jpg .png .bmp .gif .webp .tiff Image file learning material
# caution: Automatically download recognition model when running for the first time (about 200MB)
# caution: torchmust already be installed
easyocr>=1.7
