# =============================================================================
# 조선말 인공지능 조수 — 의존 라이브러리
# =============================================================================

# -----------------------------------------------------------------------------
# 핵심 (Core)
# -----------------------------------------------------------------------------
# RTX 5070 (Blackwell sm_120) 사용 시 PyTorch는 별도 설치 필요:
#   pip install torch>=2.7 --index-url https://download.pytorch.org/whl/cu128
# 일반 CUDA GPU:
#   pip install torch>=2.7
torch>=2.7
numpy>=1.26
pyyaml>=6.0

# -----------------------------------------------------------------------------
# 어표 분석기 (Tokenizer)
# -----------------------------------------------------------------------------
sentencepiece>=0.2.0

# -----------------------------------------------------------------------------
# 학습 (Training)
# -----------------------------------------------------------------------------
tensorboard>=2.16
tqdm>=4.66

# -----------------------------------------------------------------------------
# 추론 / 화면 (Inference / UI)
# -----------------------------------------------------------------------------
gradio>=6.0

# -----------------------------------------------------------------------------
# 다형식 자료 읽기 (Multi-format Data Readers)
# -----------------------------------------------------------------------------

# PDF 파일 텍스트 추출
# 사용: .pdf 파일 학습 자료
pdfplumber>=0.11

# 마이크로소프트 워드 문서 읽기
# 사용: .docx 파일 학습 자료
python-docx>=1.1

# 이미지 OCR (광학 문자 인식) — 조선말 + 영어 지원
# 사용: .jpg .png .bmp .gif .webp .tiff 이미지 파일 학습 자료
# 주의: 처음 실행 시 인식 모형 자동 내려받기 (약 200MB)
# 주의: torch가 이미 설치되어 있어야 합니다
easyocr>=1.7
