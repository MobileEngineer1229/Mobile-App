# Offline neural translation training dependencies.
#
# The current workstation already has the heavy packages below installed.
# Keep this file as the project-level dependency contract.
torch==2.11.0+cu128
transformers==5.5.0
sentencepiece==0.2.1
datasets==4.3.0
accelerate==1.13.0
protobuf==5.29.6
sacrebleu==2.6.0

