# Python Lab

Python lab is used to test model quality before mobile conversion.

It is not the final offline mobile runtime.

Planned uses:

- load the selected translation model
- test sentence translation
- test glossary post-editing
- test OCR output translation
- compare model versions with `data/evaluation/phrase_pairs.tsv`
